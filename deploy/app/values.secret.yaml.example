global:
  postgres:
    user: runner
    password: changeme

  redis:
    password: changeme

  neo4j:
    user: neo4j
    password: changeme

  s3:
    accessKeyId: changeme
    secretKey: changeme

  graphUpdate:
    # Shared bearer token expected on POST /api/graph/update.
    # Leave empty to keep the endpoint open (not recommended for production).
    token: changeme
