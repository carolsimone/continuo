apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "continuo-app.schedulesConfigMapName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "continuo-app.labels" . | nindent 4 }}
data:
  schedules.yaml: |
    {{- .Files.Get "files/schedules.yaml" | nindent 4 }}
