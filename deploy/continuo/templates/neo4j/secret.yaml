{{- if and .Values.neo4j.enabled (not .Values.neo4j.auth.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "continuo.neo4j.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "continuo.labels" . | nindent 4 }}
    app.kubernetes.io/name: neo4j
type: Opaque
stringData:
  password: {{ include "continuo.generatedSecret" (dict "root" $ "secretName" (include "continuo.neo4j.fullname" .) "key" "password") | quote }}
{{- end }}
