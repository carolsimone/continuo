# Code generated by gen-streams. DO NOT EDIT.
# Source: pkg/streams/contract.yaml

from enum import StrEnum

RELEASE_REQUESTED_V1 = "release.requested:v1"
"""Candidate release accepted by release-controller; triggers manifest load for validation."""

MANIFEST_LOADED_CANDIDATE_V1 = "manifest.loaded.candidate:v1"
"""Resolved candidate topology from topology-controller back to release-controller."""

TOPOLOGY_CONTROLLER_RELEASE_REQUESTED = "topology-controller-release-requested"
"""topology-controller consumer group on release.requested:v1."""


class ParseFailureKind(StrEnum):
    """Why topology-controller could not resolve a candidate release. Declaration order is precedence."""
    INVALID_SQL = "invalid_sql"
    UNQUALIFIED_REFERENCE = "unqualified_reference"
    INVALID_ARTIFACT = "invalid_artifact"
    INTERNAL = "internal"


PARSE_FAILURE_KIND_HEALABLE = frozenset({ParseFailureKind.INVALID_SQL, ParseFailureKind.UNQUALIFIED_REFERENCE})
"""Values of ParseFailureKind a remediation attempt can fix by changing the user's source."""
