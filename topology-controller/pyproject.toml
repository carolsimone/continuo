[project]
name = "topology-controller"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "sqlglot>=30.17.0",
    "redis>=8.1.0",
    "grpcio>=1.83.1",
    "protobuf>=7.36.0",
    "boto3>=1.43.83",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-mock>=3.12.0",
    "grpcio-tools>=1.83.1",
]

[tool.hatch.build.targets.wheel]
packages = ["config", "domain", "adapters", "service", "proto"]

[tool.pytest.ini_options]
pythonpath = [".", "proto"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
