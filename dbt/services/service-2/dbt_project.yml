name: service_2
version: '1.0.0'
config-version: 2
profile: default

model-paths: ["models"]

models:
  service_2:
    +materialized: table
    +tags: ["e2e-schedule"]
    +meta:
      owner: "test"
      criticality: "CORE"
