name: service_3_broken
version: '1.0.0'
config-version: 2
profile: default

model-paths: ["models"]

models:
  service_3_broken:
    +materialized: table
    +tags: ["e2e-schedule-failure"]
    +meta:
      owner: "test"
      criticality: "CORE"
